# Security

Report suspected vulnerabilities privately to the repository owner through GitHub's private reporting channel when available. Do not include secrets or player information in issues.

Clash API credentials belong only in ignored local configuration or environment variables. Never commit `.env`, `appsettings.Local.json`, database dumps, backups, or captured upstream responses containing private data.
